//
// link
//

import std.stdlib;
import ioring : ioring_ctx;
import vm : virtaddr;
import vfs;
import process as _ : process;
import result as _ : result;

pub fn sys_link(process mut *process, ioring_ctx mut &ctx, i32 olddirfd, virtaddr oldpathbuf, usize oldpathlen, i32 newdirfd, virtaddr newpathbuf, usize newpathlen, u64 flags) -> result
{
  return not_implemented;
}
