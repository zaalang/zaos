//
// blk result
//

pub enum result : i32
{
  ok = 0,
  not_permitted = -1,
  io_error = -5,
  no_such_device = -6,
  device_busy = -16,
  not_supported = -95,
  timed_out = -110,
  operation_cancelled = -125,

  pub fn bool(result code) -> bool
  {
    return code >= ok;
  }
}
