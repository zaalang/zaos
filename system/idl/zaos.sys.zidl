//
// zaos.sys
//

library zaos.sys;

protocol launcher
{
  launch(vector<string> argv);
}

protocol service
{
  -> terminate();
  -> detach();

  connect_to_service(string uri, channel endpoint);
}

protocol session
{
}
